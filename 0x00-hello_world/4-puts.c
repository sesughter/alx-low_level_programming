#include <stdio.h>
/**
  * main -entry point
  * Return: always 0 (success)
  */
int main(void)
{
	char hello[] = "\"Programming is like building a multilingual puzzle";

		puts(hello);

		return (0);
}
